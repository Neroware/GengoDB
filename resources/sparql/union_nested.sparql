PREFIX ex: <http://example.org/>

SELECT ?who ?what
{
    GRAPH <file://resources/ttl/coffee/coffee.ttl#rdf> {
        { ?who ex:drinks ?what }
        UNION
        { ?who ex:eats ?what }
        UNION
        { ?who ex:age ?what }
    }
}
